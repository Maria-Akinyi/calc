#include <stdio.h>
int main()
{
    float x,y;
    float calC;
    float add(float x,float y);
    printf("Enter 2 numbers: ");
    scanf("%f %f",&x, &y);
    calC=add(x,y);
    return x+y;
    printf("The sum is", calC);
    }
    float add(float x,float y){
        return x+y;
        }
